{{>layout/header}}

<table class="table">
    <thead>
    <tr>
        <th scope="col">ID</th>
        <th scope="col">이메일</th>
        <th scope="col">비밀번호</th>
    </tr>
    </thead>
    <tbody>
    <!--  객체 데이터를 사용하는 범위  -->
    {{#member}}
    <tr>
        <th>{{id}}</th>
        <td>{{email}}</td>
        <td>{{pass}}</td>

    </tr>
    {{/member}}
    <!--  객체 데이터를 사용하는 범위  -->
    </tbody>
</table>
<a href="/member/{{member.id}}/edit">수정하기</a>
<a href="/member/list">회원목록</a>

{{>layout/footer}}